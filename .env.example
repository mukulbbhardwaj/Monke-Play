# --- Frontend (Vite) ---
# API base URL. When running locally with "vercel dev", use http://localhost:3000.
# When deployed, leave empty so the app uses the same origin.
VITE_API_URL=http://localhost:3000

# --- Backend (API / Supabase) ---
# From Supabase: Project Settings → API → Project URL and anon public key.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Optional. From Supabase: Project Settings → API → service_role (secret).
# Only needed if you want to bypass RLS; anon key is enough for this app.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
